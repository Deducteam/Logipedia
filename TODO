-Write a script to put the website in production
-Fix Holide code with the double underscore thing
-Find a good way to make the translations then the exportation
-Fix Makefile dependencies with files generated by dkdep (all the dependencies for Coq, Matita are generated even though only dependencies for one system is needed)
-Dirty hack in the Makefile to handle modules name with Openstd lib
-Only Coq has been tested with Openstd lib